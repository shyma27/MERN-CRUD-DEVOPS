apiVersion: v1
kind: Service
metadata:
  name: backend-service 
  labels:
    app: backend-service 
spec:
  ports:
  - port: 8080
    targetPort: backend-app-port 
    name: backend-service-port 
  clusterIP: None
  selector:
    app: backend