apiVersion: v1
kind: Service
metadata:
  name: frontend-service 
  labels:
    app: frontend-service 
spec:
  ports:
  - port: 80
    targetPort: front-app-port 
    name: front-service-port 
  clusterIP: None
  selector:
    app: frontend